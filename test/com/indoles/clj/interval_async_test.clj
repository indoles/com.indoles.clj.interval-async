(ns com.indoles.clj.interval-async-test
  (:require [clojure.test :refer :all]
            [com.indoles.clj.interval-async :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
